{
    "name": "crowdfunding-dapp",
    "version": "1.0.0",
    "description": "Decentralized Crowdfunding DApp for Blockchain Course",
    "main": "index.js",
    "scripts": {
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.js --network sepolia",
        "test": "hardhat test"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.1",
        "dotenv": "^16.3.1"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^3.0.0",
        "hardhat": "^2.19.1"
    }
}